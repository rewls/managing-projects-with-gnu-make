#include <stdio.h>
#include <stdlib.h>
#include <lexer.h>
#include <counter.h>

int main(void)
{
	int counts[4];
	counter(counts);
	printf("%d %d %d %d\n", fee_count, fie_count, foe_count, fum_count);
	exit(EXIT_SUCCESS);
}
